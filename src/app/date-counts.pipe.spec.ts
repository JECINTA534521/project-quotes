import { DateCountsPipe } from './date-counts.pipe';

describe('DateCountsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCountsPipe();
    expect(pipe).toBeTruthy();
  });
});
