import { DateCountPipe } from './date-counts.pipe';

describe('DateCountsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCountPipe();
    expect(pipe).toBeTruthy();
  });
});
